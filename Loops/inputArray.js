//Basic of how to print the element of array using loop
const vacationSpots = ['Bali', 'Paris', 'Tulum'];

for (let i = 0; i < vacationSpots.length; i++)  //.length property to take the length
{
    console.log('I would love to visit ' + vacationSpots[i]);
}